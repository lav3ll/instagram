const database = require('./database');

test('it contains mysql and config imports', () => {
  const mysqlTest = '';
  const myconfigTest = config;
  expect(mysqlTest, myconfigTest).toBeDentified();
});
